"""
====================================================================
文件用途：CRUD 数据访问层聚合出口（crud 包的门面）
====================================================================
作用：
    将各表的 CRUD 模块聚合导出，调用方统一写：
    `from app.crud import tasks, templates, users, agent_logs`
依赖：
    - app.crud 下的四个子模块
调用方：
    - app/api/routes.py（后续：API 路由）
    - app/agents/*（后续：LangGraph 各节点）
    - scripts/init_*.py（后续：初始化脚本）
说明：
    - CRUD 层只做“单表数据访问”，不包含业务编排逻辑；
      跨表/多步骤逻辑应放在 services 层或 Agent 节点中。
====================================================================
"""
