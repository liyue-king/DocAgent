"""
====================================================================
文件用途：智能体编排层聚合出口（agents 包门面）
====================================================================
作用：
    集中导出 LangGraph 状态机构建/执行入口，供上层（Celery 任务、
    API 路由、冒烟测试）直接调用：
        from app.agents import build_graph, run_agent
====================================================================
"""

from app.agents.graph import build_graph, run_agent  # 状态机构建与执行入口

# 对外公开符号白名单
__all__ = ["build_graph", "run_agent"]
