# DocAgent 其他页面提示词（延续 screen.png 风格）

> 参考设计风格：浅灰白背景、纯白卡片、靛蓝主按钮、大圆角、柔和阴影、文档mockup悬浮、克制现代的中文 SaaS 感。以下每个页面都是可直接复制给 AI 的独立提示词。

---

## 通用视觉基线（每个提示词都默认遵循）

```text
通用视觉约束：
- 背景：#F8FAFC 或 #F9FAFB 浅灰白，避免纯白刺眼
- 卡片：白色 #FFFFFF，圆角 12px，阴影 0 4px 20px rgba(0,0,0,0.06)
- 主按钮：靛蓝渐变 #4F46E5 → #4338CA，圆角 10px，文字白色
- 次按钮：白色背景 + 1px #E2E8F0 边框，hover 背景变浅灰
- 标题：#111827，正文：#6B7280，强调文字：#4F46E5
- 字体：中文使用 "PingFang SC", "Microsoft YaHei", sans-serif
- 图标：线性图标，2px 描边，颜色 #6B7280 或 #4F46E5
- 间距： generous，模块之间 80-120px
- 动效：0.2s ease 悬浮效果，卡片 hover 轻微上浮 2px
```

---

## 1. 上传与需求提交页（Upload + Prompt）

```text
设计 DocAgent 的上传与需求提交页。延续 screen.png 的浅灰白 SaaS 风格。页面顶部保留相同导航栏（Logo + 功能/定价/模板 + 蓝色"立即使用"按钮）。主内容区居中，max-width 800px。

上半部分是一个大的白色拖拽上传卡片，内部有虚线边框的拖拽区（hover 变蓝），中央是云上传图标和主标题"上传 Word 文档"，副标题"支持 .docx 格式，最大 20MB"。已选择文件后显示文件卡片：文件名、大小、删除按钮。

下半部分是多行 prompt 输入区，白色卡片，带标签"你想怎么排版？"，占位符为"例如：把这篇论文按本科毕业论文格式排版，标题黑体三号，正文宋体小四，1.5倍行距"。输入框下方有一排快捷标签按钮："学术论文"、"商务报告"、"政府公文"、"简历"，点击自动填入示例 prompt。

底部是居中的大号主按钮"开始智能排版"，仅在文件和 prompt 均非空时高亮，否则置灰。右侧有一个悬浮的文档 mockup 小插图，显示 Word 图标和 AI 分析光效。整体干净、聚焦、有引导感。
```

---

## 2. 处理中页（Processing）

```text
设计 DocAgent 的任务处理中页面。延续 screen.png 的浅灰白 SaaS 风格，但增加一点科技感的深色终端元素作为视觉焦点。页面顶部保留导航栏。

主内容区居中，max-width 720px。中央是一个白色大卡片，顶部显示"正在处理文档..."和当前步骤文字"正在修改第 3 章标题"。

中间是分段进度条，分为四段：RAG 检索（0-30%）、AI 规划（30-60%）、文档修改（60-90%）、质量校验（90-100%）。当前段填充靛蓝色，已完成段显示蓝色对勾，未开始段为浅灰。进度条上方有百分比数字。

下方是一个深色日志终端区域（#0F172A 背景，#34D399 绿色文字），高度 220px，圆角 12px，自动滚动。日志格式为"[10:23:14] 检索完成：命中学术论文模板"。终端左上角有三个小圆点（红/黄/绿），模仿窗口控制按钮。

卡片底部有一行小字"预计还需 45 秒"和取消任务按钮。整体氛围专注、专业，让用户感知 AI 正在工作。
```

---

## 3. 重试中页（Retrying）

```text
设计 DocAgent 的重试中页面。延续浅灰白 SaaS 风格，但在处理中页基础上加入温暖的警告状态。页面顶部保留导航栏。

主内容区居中，max-width 720px。中央白色卡片顶部有一条浅黄色警告条（#FEF3C7 背景，#B45309 文字），文字"校验发现少量偏差，AI 正在进行第 2 次优化"。

进度条使用琥珀黄（#F59E0B）作为当前段颜色，并带有柔和的呼吸动画（opacity 0.7 ↔ 1，0.8s 循环）。进度条下方的步骤文字为"正在针对 missed 段落生成修补指令"。

日志终端继续滚动，新增黄色 WARNING 级别的日志行，例如"[10:24:05] WARNING: 第 7 段行距未达标，触发增量修补"。

卡片底部提示"这是正常的质量校验过程，请稍候"。整体不慌张，传递"AI 在自我修正"的可靠感。
```

---

## 4. 成功结果页（Success）

```text
设计 DocAgent 的处理成功结果页。延续 screen.png 的浅灰白 SaaS 风格，情绪明亮积极。页面顶部保留导航栏。

主内容区居中，max-width 640px。中央是一个白色卡片，内部从上到下：
- 一个绿色成功对勾动画（SVG 描边动画，#10B981）
- 大标题"排版完成"，#111827
- 副标题"共处理 24 个段落，耗时 1 分 32 秒"
- 一个文件信息卡片：显示输出文件名、文件大小、生成时间
- 大号主按钮"下载文档"（靛蓝渐变）
- 次按钮"再来一单"
- 底部小字"下载链接 5 分钟后过期，请及时保存"

背景中有一个淡淡的文档悬浮插图，带成功光效。整体像是一个干净的完成确认页，给用户即时满足感。
```

---

## 5. 失败/错误页（Error）

```text
设计 DocAgent 的任务失败页面。延续 screen.png 的浅灰白 SaaS 风格，但情绪转为沉稳清晰，不吓人。页面顶部保留导航栏。

主内容区居中，max-width 640px。中央是一个白色卡片，左侧有一个红色错误图标（圆形，#FEE2E2 背景，#EF4444 图标），右侧是错误信息：
- 标题"排版未能完成"
- 错误码和友好说明："错误码 4002 - 校验后覆盖率未达标，可下载当前结果或重试"
- 一个可展开的"查看详细日志"区域
- 两个按钮并排：主按钮"重新处理"（靛蓝），次按钮"下载当前结果"（白色边框）
- 最下方有"返回首页"文字链接

整体用色克制，红色只用于图标和少量强调文字，避免大面积红色造成焦虑。传递"问题可控，有退路"的感觉。
```

---

## 6. 模板选择页（Templates）

```text
设计 DocAgent 的模板选择页。延续 screen.png 的浅灰白 SaaS 风格。页面顶部保留导航栏。

主内容区居中，max-width 1000px。页面标题"选择一个排版模板"，副标题"DocAgent 会根据你的文档内容智能推荐最合适的模板"。

下方是一个 3 列网格的模板卡片列表，每张卡片：
- 顶部是模板图标（学术帽、公文、报告等线性图标）
- 模板名称："学术论文"、"商务标书"、"政府公文"等
- 一句话描述："适用于本科/硕士毕业论文..."
- 标签：如"最常用"、"高匹配"、"通用"
- hover 时卡片上浮 4px，边框变为靛蓝色

第一行可以有一个"智能推荐"大卡片，跨两列，显示推荐模板和置信度分数条。底部有一个"使用选中模板"主按钮。

整体像是一个精选工具箱，让用户有选择权但不被 overwhelm。
```

---

## 7. 任务历史页（Task History）

```text
设计 DocAgent 的任务历史页。延续 screen.png 的浅灰白 SaaS 风格。页面顶部保留导航栏。

主内容区居中，max-width 1000px。页面标题"我的任务"，右上角有"新建任务"主按钮。

主体是一个白色卡片内的任务列表表格，列包括：文件名、提交时间、使用模板、状态、操作。状态用彩色小标签表示：成功（绿色）、处理中（蓝色动画点）、失败（红色）、已过期（灰色）。

每行 hover 时背景变浅灰。操作列根据状态显示不同按钮：成功状态显示"下载"，处理中显示"查看"，失败显示"重试"，已过期显示"删除"。

表格上方有一个搜索框和状态筛选标签（全部/处理中/成功/失败）。空状态时显示一个友好的空状态插图和"还没有任务，去上传一个文档吧"提示。

整体像是一个干净的后台管理页，信息清晰，操作明确。
```

---

## 8. 定价页（Pricing）

```text
设计 DocAgent 的定价页。延续 screen.png 的浅灰白 SaaS 风格，保持 landing 页的叙事感。页面顶部保留导航栏。

主内容区居中，max-width 1000px。页面标题"简单透明的定价"，副标题"按文档处理次数付费，无隐藏费用"。

下方是 3 列定价卡片：
- 免费版：0 元/月，10 次/月，基础模板
- 专业版：推荐标签，29 元/月，100 次/月，全部模板 + 优先处理
- 团队版：99 元/月，500 次/月，API 接入 + 团队协作

中间的专业版卡片稍微放大，顶部有"最受欢迎"蓝色标签。每张卡片内有价格大字号显示、功能列表（带蓝色对勾）、底部 CTA 按钮。

底部有一个 FAQ 折叠区域，回答"如何计费？"、"支持退款吗？"等问题。整体商业感强但不压迫，用蓝色强调信任。
```

---

## 9. 关于/团队页（About）

```text
设计 DocAgent 的关于我们页面。延续 screen.png 的浅灰白 SaaS 风格。页面顶部保留导航栏。

主内容区居中，max-width 900px。页面标题"让 AI 重新定义文档排版"，副标题讲述产品使命："我们相信每个人都应该拥有专业、规范的文档，而不需要成为排版专家。"

下方分三块内容：
- 产品故事：一段文字 + 一张团队/工作场景图片
- 核心优势：三个白色卡片，分别说明"多智能体协作"、"RAG 模板检索"、"自动质量校验"，每个卡片带图标和简短说明
- 技术栈展示：一行技术 Logo（Vue3、FastAPI、LangGraph、ChromaDB 等），灰度显示

底部有一个 CTA 区块"准备好体验了吗？"，配蓝色"免费开始"按钮。整体像是一个可信的品牌故事页，不过度营销。
```

---

## 10. 登录页（Login）

```text
设计 DocAgent 的登录页。延续 screen.png 的浅灰白 SaaS 风格，保持简洁克制。

页面采用居中单卡片布局，背景为浅灰白（#F8FAFC）。中央是一个白色登录卡片，max-width 420px，圆角 16px，带柔和阴影。卡片顶部中央是 DocAgent Logo 和产品名，下方是标题"欢迎回来"和副标题"登录后继续处理你的文档"。

表单包含：
- 邮箱输入框：带信封图标，占位符"your@email.com"
- 密码输入框：带锁图标，右侧有显示/隐藏密码的眼睛图标
- 记住我复选框 + 忘记密码链接（右对齐，蓝色文字）
- 大号主按钮"登录"（靛蓝渐变，满宽）

按钮下方是分隔线"或"，再下面是第三方登录按钮："使用 Google 登录"、"使用微信登录"（白色背景 + 图标 + 灰色边框）。

卡片底部有一行文字"还没有账号？立即注册"，"立即注册"为蓝色文字链接。

整体不要大面积插图，保持表单聚焦。输入框聚焦时边框变靛蓝，错误时边框变红并显示下方错误提示。风格与主站一致：干净、现代、可信。
```

---

## 11. 注册页（Register）

```text
设计 DocAgent 的注册页。延续 screen.png 的浅灰白 SaaS 风格，与登录页视觉统一。

页面采用居中单卡片布局，背景为浅灰白（#F8FAFC）。中央是一个白色注册卡片，max-width 420px，圆角 16px，带柔和阴影。卡片顶部是 Logo 和标题"创建账号"，副标题"开始免费体验 AI 文档排版"。

表单包含：
- 邮箱输入框：带信封图标，占位符"your@email.com"
- 密码输入框：带锁图标，右侧显示/隐藏切换
- 确认密码输入框
- 密码强度条：根据输入显示弱/中/强（红/黄/绿）
- 用户协议复选框："我已阅读并同意《用户协议》和《隐私政策》"，链接为蓝色
- 大号主按钮"注册"（靛蓝渐变，满宽）

按钮下方是"或"分隔线和第三方注册按钮："使用 Google 注册"、"使用微信注册"。

卡片底部有一行文字"已有账号？立即登录"，"立即登录"为蓝色文字链接。

注册成功后卡片内容平滑切换为成功状态：绿色对勾 + "注册成功，正在跳转..."文案。整体保持简洁，避免过多装饰，突出表单和转化。
```

---

## 12. 通用组件提示词

```text
为 DocAgent 设计一套通用 UI 组件库，延续 screen.png 的浅灰白 SaaS 风格。需要包含：

1. 导航栏：左侧 Logo + 产品名，中间导航链接（功能/定价/模板/关于），右侧"登录"文字按钮和"立即使用"蓝色主按钮。滚动时导航栏背景变为半透明白色 + 底部细线。

2. 页脚：深色文字链接（隐私政策、服务条款、帮助中心），底部版权信息"© 2024 DocAgent"。极简，无多余装饰。

3. 按钮组件：主按钮（靛蓝渐变、白色文字、圆角 10px），次按钮（白底灰边），文字按钮（蓝色文字 + hover 下划线）。

4. 卡片组件：白色背景、12px 圆角、柔和阴影、hover 上浮 2px。

5. 输入框组件：白底、1px #E2E8F0 边框、聚焦时靛蓝边框、圆角 8px。

6. 标签组件：小圆角 pill 形状，有成功/警告/错误/默认四种颜色。

7. 空状态组件：居中插图 + 标题 + 说明 + 操作按钮。

所有组件在浅灰白背景上展示，间距 generous，风格统一克制。
```

---

## 使用建议

1. 直接复制对应页面的提示词给 AI 生成代码。
2. 把"通用视觉基线"追加到每个提示词末尾，确保风格一致。
3. 如果 AI 生成后风格偏离，用"更浅的阴影"、"更大的圆角"、"更克制的颜色"来纠正。
4. 建议先生成上传页和处理中页（核心流程），再补历史页和定价页。
