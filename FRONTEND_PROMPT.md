# DocAgent 前端视图生成提示词

> 用途：将本提示词直接喂给 AI（如 Cursor / Cline / Claude / Kimi），用于生成 DocAgent 项目的前端 Vue3 代码。目标不是“功能可用”，而是**视觉精致、动效克制、体验流畅**。

---

## 1. 项目背景

- **产品**：DocAgent - 基于多智能体协作的智能文档处理平台
- **技术栈**：Vue3 + Element-Plus + Vite + Axios
- **核心流程**：用户上传 `.docx` → 输入自然语言需求 → 后端多 Agent 处理 → 前端轮询状态 → 下载结果
- **参考蓝图**：`PROJECT_BLUEPRINT.md` 第 8 章（UI 状态机与组件结构）

---

## 2. 设计方向

整体风格：**“冷静的魔法感”**

不是冷冰冰的后台工具，也不是过度活泼的玩具。它应该像一个安静的文档专家：

- 界面干净、信息密度适中
- 状态转换用动画表达，而非文字堆砌
- 黑色日志终端是视觉锚点，营造“Agent 正在思考”的沉浸感
- 成功 / 失败状态情绪明确，给用户即时反馈

---

## 3. 视觉系统

### 3.1 色彩

```text
背景主色：  #F7F8FA  （浅灰白，缓解长时间注视疲劳）
卡片背景：  #FFFFFF  （纯白，带 1px #E8EAED 边框）
主品牌色：  #2563EB  （靛蓝，用于主按钮、进度条、高亮）
成功色：    #10B981  （ emerald 绿）
警告色：    #F59E0B  （琥珀黄，用于重试中）
错误色：    #EF4444  （砖红）
文本主色：  #1F2937  （深灰黑）
文本次色：  #6B7280  （中灰，用于说明、占位符）
终端背景：  #0F172A  （ slate 深色）
终端文字：  #34D399  （ terminal 绿）
终端次要：  #94A3B8  （ slate 400，时间戳、INFO 等级）
```

### 3.2 字体

- 中文：`"PingFang SC", "Microsoft YaHei", sans-serif`
- 等宽日志：`"JetBrains Mono", "SF Mono", "Consolas", monospace`
- 标题字重：600；正文字重：400

### 3.3 圆角与阴影

- 卡片圆角：`12px`
- 按钮圆角：`8px`
- 小标签圆角：`6px`
- 卡片阴影：`0 1px 3px rgba(0,0,0,0.06), 0 4px 12px rgba(0,0,0,0.04)`
- 悬浮阴影：`0 8px 24px rgba(0,0,0,0.08)`

---

## 4. 布局架构

```text
┌─────────────────────────────────────────────┐
│  顶部导航栏（可选）：DocAgent Logo + 版本标签    │
├─────────────────────────────────────────────┤
│                                             │
│           主内容区（居中，max-width 720px）    │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  标题区：一键排版，像变魔术一样        │   │
│  │  副标题：上传 Word，AI 自动完成格式规范  │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  动态状态卡片（根据状态切换内容）       │   │
│  │  - 上传区 + Prompt 输入                │   │
│  │  - 进度条 + 日志终端                   │   │
│  │  - 成功 / 失败结果页                   │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  底部：安全提示（24h 自动删除 / 原文件不丢失） │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 5. 状态视图设计（6 种状态）

### 5.1 空闲状态（Idle）

- **拖拽上传区**：大号虚线边框区域，hover 时边框变蓝、背景微微变蓝（`#EFF6FF`）
  - 内部图标：上传云图标（Element-Plus `Upload` 或自定义 SVG）
  - 主文案：**“拖拽 Word 文档到此处，或点击上传”**
  - 次文案：仅支持 `.docx`，最大 20MB
- **Prompt 输入框**：多行文本域，占位符给出示例：
  - `示例："把这篇论文按本科毕业论文格式排版，标题黑体三号，正文宋体小四，1.5倍行距"`
- **开始按钮**：
  - 文案：**“开始魔法处理”**
  - 文件和 prompt 均非空时高亮（主品牌色渐变 `#2563EB → #1D4ED8`）
  - 未满足条件时置灰禁用

### 5.2 上传中（Uploading）

- 文件卡片显示：文件名 + 文件大小 + 圆形进度环
- 进度环内部显示百分比
- 取消按钮：小号文字按钮

### 5.3 处理中（Processing）

- **分段进度条**（视觉重点）：
  - 检索 0-30%：标签 `RAG 检索`
  - 规划 30-60%：标签 `AI 规划`
  - 执行 60-90%：标签 `文档修改`
  - 校验 90-100%：标签 `质量校验`
  - 当前激活段用品牌色填充，已完成段用成功色，未开始段用浅灰
- **当前步骤文字**：如 `正在修改第 3 章标题`，位于进度条下方
- **日志终端**（黑底绿字）：
  - 高度 `200px`，圆角 `12px`
  - 新日志自动滚动到底部
  - 时间戳用终端次要色，内容用终端绿
  - 支持 WARNING（黄色）/ ERROR（红色）等级颜色
  - 示例日志：
    ```text
    [10:23:14] 检索完成：命中「学术论文」模板（置信度 0.87）
    [10:23:15] Planner 进入确定性快路径（0 token）
    [10:23:18] Executor 开始执行 24 条原子指令
    [10:23:19] 正在修改第 3 章标题
    ```

### 5.4 重试中（Retrying）

- 进度条进入**黄色呼吸动画**（不刺眼，0.8s 缓动）
- 卡片顶部出现警告条：**“校验发现少量偏差，AI 正在二次优化（第 2 次）”**
- 日志继续滚动，新增 `retry` 相关记录

### 5.5 成功状态（Success）

- 中央绿色对勾动画（SVG stroke-dashoffset 描边动画，约 0.6s）
- 大标题：**“排版完成”**
- 副标题：显示处理耗时、Token 消耗（可选）
- **主按钮**：`下载文档`，大号圆角按钮，成功色渐变
- **次按钮**：`再来一单`，重置整个表单
- 5 分钟后下载链接过期提示（小字）

### 5.6 失败状态（Error）

- 红色错误卡片，带错误图标
- 显示错误码 + 用户友好文案：
  - 例：`4002 - 排版校验未通过，可下载当前结果或重试`
- **复制错误日志**按钮
- **重试**按钮（重新提交同一文件和 prompt）
- **返回**按钮（重置到 idle）

---

## 6. 动效规范

所有动画使用 `cubic-bezier(0.4, 0, 0.2, 1)`（Material 标准缓动）

| 元素 | 动画 | 时长 |
| :--- | :--- | :--- |
| 卡片状态切换 | fade + scale(0.98 → 1) | 300ms |
| 拖拽区 hover | border-color + background-color | 200ms |
| 进度条填充 | width 过渡 | 400ms |
| 重试呼吸 | opacity 0.6 ↔ 1 + shadow 脉冲 | 800ms 循环 |
| 成功对勾 | stroke-dashoffset 描边 | 600ms |
| 日志新增 | 新行从下方滑入 | 150ms |
| 按钮 hover | translateY(-1px) + shadow 加深 | 150ms |

---

## 7. 组件结构

```text
frontend/src/
├── App.vue                      # 根组件：状态机 + 整体布局
├── components/
│   ├── UploadZone.vue           # 拖拽上传 + 文件校验
│   ├── PromptInput.vue          # 自然语言需求输入
│   ├── ProgressBar.vue          # 分段进度条（含重试呼吸态）
│   ├── LogTerminal.vue          # 黑底绿字滚动日志终端
│   ├── SuccessPanel.vue         # 下载 + 再来一单
│   ├── ErrorPanel.vue           # 错误卡片 + 复制日志
│   └── AnimatedCheck.vue        # 成功对勾 SVG 动画
├── api/client.js                # axios 封装
├── composables/useTaskPolling.js # 轮询逻辑
└── styles/
    └── variables.css            # CSS 变量（颜色、圆角、阴影）
```

---

## 8. 交互细节

- 轮询频率：每 2 秒一次 `GET /api/v1/task/{id}`
- `status === success` 时停止轮询，显示下载链接
- `status === failed / expired` 时停止轮询，显示错误视图
- 页面关闭或刷新时，不中断后端任务（下次打开可继续查状态，可选 P1）
- 上传区支持点击选择文件和拖拽两种方式
- Prompt 输入框支持 `Enter` 不提交，`Ctrl+Enter` 提交（避免误触）

---

## 9. 输出要求

请生成一个完整可运行的 Vue3 单文件应用入口 `App.vue`，并拆分上述组件。要求：

1. 使用 Element-Plus 组件，但**不要直接使用默认样式**，通过 CSS 变量和自定义 class 覆盖视觉风格。
2. 所有颜色、圆角、阴影抽取到 `styles/variables.css`。
3. 动画使用 Vue `<Transition>` + CSS，避免引入重型动画库。
4. 日志终端组件支持传入日志数组，自动滚动，新日志有滑入效果。
5. 进度条组件支持分段显示和重试呼吸态。
6. 代码注释使用中文。
7. 不引入与项目无关的第三方 UI 库。

---

## 10. 可直接复制给 AI 的一句话指令

```text
请根据 FRONTEND_PROMPT.md 中的设计系统、状态视图、动效规范和组件结构，为 DocAgent 项目生成一套 Vue3 + Element-Plus + Vite 的前端代码。要求界面风格为"冷静的魔法感"：干净、精致、动效克制。重点实现 6 种状态视图（idle/uploading/processing/retrying/success/error）、黑底绿字滚动日志终端、分段进度条、成功对勾动画。所有颜色圆角阴影抽取到 CSS 变量，组件拆分清晰，代码含中文注释。
```
